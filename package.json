{
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "format": "prettier --no-semi --single-quote --write {examples,packages}/**/*.js"
  },
  "repository": "mdx-js/mdx",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^2.5.1",
    "prettier": "^1.11.1"
  }
}
