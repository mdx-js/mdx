{
  extends: ['config:base', ':preserveSemverRanges'],
  schedule: 'before 3am on Monday',
  packageRules: [
    {
      matchPackageNames: [
        // `nanoid@5` does not work on Node 16.
        'nanoid'
      ],
      enabled: false
    },
    {
      matchPackageNames: [
        // `periscopic@4` does not work at all.
        'periscopic'
      ],
      enabled: false
    },
    {
      matchPackageNames: [
        // `puppeteer-core@21` does not work with `chrome-aws-lambda` types.
        'puppeteer-core'
      ],
      enabled: false
    }
  ]
}
