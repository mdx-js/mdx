import {TwitterTweetEmbed} from 'react-twitter-embed'

export default ({children}) => (
  <div
    style={{
      margin: '2em auto',
      maxWidth: '40rem',
      fontFamily: 'system-ui'
    }}
  >
    {children}
  </div>
)

# Hello, world!

Here’s a tweet:

<TwitterTweetEmbed
  tweetId={'1116723357410447360'}
/>
