language: node_js
node_js:
  - 8
  - 10
git:
  depth: 5
cache:
  directories:
    - node_modules
before_deploy: npm install now --no-save
deploy:
  - provider: script
    script: npm run docs:build && now dist --token $NOW_TOKEN
    skip_cleanup: true
    on:
      all_branches: true
      master: false
  - provider: script
    script: npm run docs:build && now dist --token $NOW_TOKEN && now alias --token $NOW_TOKEN
    skip_cleanup: true
    on:
      master: true
